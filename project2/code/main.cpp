#include "project2.hpp"

int main(int argc, char const *argv[]) {
  methods object;

  double eps = pow(10,-8);
  object.n = atoi(argv[1]);
  object.A = mat(n, n)
  object.offA = 100;
  object.

  while (offA > eps){
    object.jacobi(A)
    object.offA(A)
  }

  return 0;
}
